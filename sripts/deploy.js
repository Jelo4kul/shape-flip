const { task } = require("hardhat/config");

task("check-balance", "Checks the balance of a wallet account")
.setAction();

task("deploy", "Deploys a contract")
.setAction();

////git config --global --edit